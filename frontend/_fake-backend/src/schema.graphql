type Query {
  fetchOffers(customerId: String!): [Offer!]!
}

type Offer {
  title: String!
  url: String!
  imageUrl: String!
  features: [Feature!]!
}

type Feature {
  title: String!
  lines: [String!]!
}
